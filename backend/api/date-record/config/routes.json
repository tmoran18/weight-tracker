{
  "routes": [
    {
      "method": "GET",
      "path": "/date-records",
      "handler": "date-record.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/date-records/count",
      "handler": "date-record.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/date-records/:id",
      "handler": "date-record.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/date-records",
      "handler": "date-record.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/date-records/:id",
      "handler": "date-record.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/date-records/:id",
      "handler": "date-record.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
